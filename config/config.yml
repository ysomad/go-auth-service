app:
  name: 'go-auth-service'
  version: '1.0.0'

http:
  port: '8080'

logger:
  log_level: 'debug'
  rollbar_env: 'go-auth-service-template'

postgres:
  pool_max: 2

mongodb:
  database: 'authService'

cache:
  ttl: 30s

session:
  ttl: 1m

jwt:
  ttl: 15m
  signing_key: 'secret'
